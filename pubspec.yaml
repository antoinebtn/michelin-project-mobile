name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10
  dio: ^5.7.0               # Pour les requêtes HTTP performantes
  flutter_secure_storage: ^9.2.2 # Pour stocker le token de manière chiffrée

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
